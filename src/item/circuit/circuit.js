import { Settings } from '../../common.js'
Settings.resDB.circuit.name = 'circuit'
Settings.resDB.circuit.type = 'item'
Settings.resDB.circuit.cost = [{ id: Settings.resDB.iron_plate.id, n: 1 }, { id: Settings.resDB.copper_cable.id, n: 3 }]
