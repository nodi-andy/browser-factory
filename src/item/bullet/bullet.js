import { Settings } from '../../common.js'
Settings.resDB.bullet.name = 'bullet'
Settings.resDB.bullet.type = 'item'
Settings.resDB.bullet.cost = [{ id: Settings.resDB.iron_plate.id, n: 1 }]
Settings.resDB.bullet.lock = 1
